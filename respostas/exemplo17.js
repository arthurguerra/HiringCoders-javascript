// Crie uma função que recebe 2 parâmetros e retorna a multiplicação entre else.

function multiplicar(num1, num2) {
    let multiplicação = num1 * num2

    return `${num1} * ${num2} = ${multiplicação}`
}

console.log(multiplicar(1.75, -5))
