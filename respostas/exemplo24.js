// Crie um algoritmo que converte dias em horas, quando recebe um número de dias.

function converterDiasParaHoras(dias) {
    let horas = dias * 24    

    console.log(`${dias} correspondem à ${horas} horas.`)
}

converterDiasParaHoras(30)